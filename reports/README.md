# reports/

Human-readable, formatted reports generated from AI output (e.g. Markdown data dictionaries, DQ executive summaries, release impact reports) — distinct from the raw machine-readable AI output in `outputs/`.

Runtime-generated reports are not committed (see `.gitignore`); this folder exists so generated reports have one consistent destination once report generation is implemented.
