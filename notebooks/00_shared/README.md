# notebooks/00_shared/

Reusable utility notebooks meant to be `%run` from other notebooks (not run standalone as a pipeline step), e.g. `01_gemini_client.py`. Numbered `00_` so it sorts before the phase folders (`01_development/`, `02_ai_metadata/`, ...) that depend on it.
