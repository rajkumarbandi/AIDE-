# outputs/

Raw, machine-readable AI-generated output (JSON) produced by pipeline runs — e.g. generated table metadata, DQ findings, release impact analysis — before formatting into human-readable reports (see `reports/`).

Runtime output is not committed (see `.gitignore`), except for `samples/`, which holds small, illustrative example outputs checked into git for documentation purposes.
