# scripts/

Operational/setup scripts that support development but aren't part of the shipped pipeline logic in `src/` — e.g. local environment setup, dataset preparation helpers, one-off maintenance scripts.

No scripts exist yet.
