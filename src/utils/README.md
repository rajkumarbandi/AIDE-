# src/utils/

Reusable, cross-cutting utility code intended to be shared by all three AIDE capabilities (metadata generation, DQ/RCA, release impact analysis) — e.g. config loading, structured logging, path helpers.

No modules exist yet. This folder is reserved so utilities have one obvious home instead of being duplicated per capability.
